require "spec_helper"

RSpec.describe Mail::Secure do
  it "has a version number" do
    expect(Mail::Secure::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
